if('<%= params[:submit]%>' != "")
{
	window.location = "<%= new_user_url %>";
}else
{
	$('div#quiz').html("<%= escape_javascript(render('shared/questionnaire',:quiz_route => root_path , :quiz => @quiz, :quiziations => @quiziations))%>");
}
