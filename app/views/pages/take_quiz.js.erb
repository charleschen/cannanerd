// alert('<%= "should work #{params[:forward_button]}" %>');
if('<%= params[:submit]%>' != "")
{
	window.location = "<%= new_user_url %>";
}else
{
	$('div#questionnaire_div').html("<%= escape_javascript(render('shared/questionnaire',:quiz_route => take_quiz_path , :quiz => @quiz, :quiziations => @quiziations))%>");
}