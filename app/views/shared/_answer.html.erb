<% display_tags ||= false %>

<li class='questionnaire_answer'>
	<p><%= answer.content %></p>
	<% if display_tags %>
	<span>
		<ul class='tags'>
		<% answer.tags.each do |tag| %>
			<li class='tag'><%= tag.name %></li>
		<% end %>
		</ul>
	</span>
	<% end %>
</li>